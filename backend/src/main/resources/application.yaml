server:
  port: 8090

spring:
  application:
    name: cinema_backend
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/cinema?serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true